# OFFICIAL-SENSITIVE MVP Vercel build fix

This ZIP contains the files needed to fix the Vercel error:

Rollup failed to resolve import /src/main.jsx from /index.html

Upload these files to the root of the GitHub repository:

- index.html
- package.json
- vite.config.js
- src/main.jsx

Do not delete your existing src/App.jsx or src/style.css.
Make sure the folder is named exactly src, all lowercase.
